# =============================================================================
# Keen-One Astronomy Stack - Environment Configuration
# =============================================================================
# Copy this file to .env and customize as needed
# Most settings have sensible defaults and can be left unchanged
#
# IMPORTANT: Never commit .env to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Timezone
# -----------------------------------------------------------------------------
# Your local timezone (used for time display in KStars/Stellarium)
TIMEZONE=Europe/London

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------
# Log verbosity level:
#   0 = ERROR only (minimal output)
#   1 = WARN  (errors and warnings)
#   2 = INFO  (normal operation - recommended for production)
#   3 = DEBUG (verbose - useful for troubleshooting)
LOG_LEVEL=3

# -----------------------------------------------------------------------------
# Mount Connection
# -----------------------------------------------------------------------------
# Leave MOUNT_IP blank for auto-discovery (recommended)
# Set manually if auto-discovery doesn't find your mount
#
# Example: MOUNT_IP=192.168.0.86
MOUNT_IP=

# OnStepX default port (usually doesn't need changing)
MOUNT_PORT=9999

# INDI driver name (match your mount type)
MOUNT_DRIVER=LX200 OnStep

# -----------------------------------------------------------------------------
# INDI Server
# -----------------------------------------------------------------------------
# These usually don't need changing (Docker internal networking)
INDI_HOST=indiserver
INDI_PORT=7624

# -----------------------------------------------------------------------------
# Location
# -----------------------------------------------------------------------------
# Set to 'true' to automatically get location from mount's GPS
# Set to 'false' and provide manual coordinates below if no GPS
USE_GPS_LOCATION=true

# Manual location (used if GPS unavailable or USE_GPS_LOCATION=false)
# Leave blank to use defaults
LATITUDE=
LONGITUDE=
ELEVATION=

# -----------------------------------------------------------------------------
# Self-Test Settings
# -----------------------------------------------------------------------------
# Run automatic diagnostics on first boot
SELF_TEST_ON_BOOT=true

# Run diagnostics on every container restart (can slow startup)
SELF_TEST_ON_RESTART=false

# =============================================================================
# WiFi Configuration (Reference for OnStepX Config.h)
# =============================================================================
# Your home WiFi credentials - used when configuring OnStepX firmware
# These are NOT used by Docker, just documented here for reference
# WIFI_SSID=YourWiFiName
# WIFI_PASSWORD=YourWiFiPassword

# =============================================================================
# Advanced / Portainer Users
# =============================================================================
# If deploying via Portainer, you can set these in the Portainer UI
# instead of using this file. The docker-compose.yml has defaults for all
# settings, so you only need to override what you want to change.
#
# Minimum required for Portainer deployment:
#   - Just deploy the stack, defaults will work for most setups
#   - Set MOUNT_IP if your mount isn't auto-discovered
#   - Set TIMEZONE to your local timezone
# =============================================================================
